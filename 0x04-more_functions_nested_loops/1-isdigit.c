#include "main.h"

/**
 * _isdigit - Point to check the code
 * @c: character to be checked
 * Return: Always 0
 */
int _isdigit(int c)
{
	if (c >= 48 && c <= 57)
		return (1);
	else
		return (0);
	return (0);
}
